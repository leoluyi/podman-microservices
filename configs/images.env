# ============================================================================
# 容器鏡像配置
# ============================================================================
# 此檔案定義所有服務使用的容器鏡像
# 
# 使用方式：
# 1. 修改鏡像位置和版本
# 2. 更新對應的 .container 檔案中的 Image= 參數
#
# 鏡像來源範例：
# - Docker Hub:     docker.io/library/nginx:alpine
# - Quay.io:        quay.io/your-org/api-user:v1.0.0
# - 私有 Registry:  registry.example.com/api-user:latest
# - 本地建置:        localhost/api-user:latest
# ============================================================================

# ============================================================================
# Backend APIs
# ============================================================================

# API User Service
API_USER_IMAGE=docker.io/library/nginx:alpine
API_USER_TAG=latest

# API Order Service
API_ORDER_IMAGE=docker.io/library/nginx:alpine
API_ORDER_TAG=latest

# API Product Service
API_PRODUCT_IMAGE=docker.io/library/nginx:alpine
API_PRODUCT_TAG=latest

# ============================================================================
# Gateway Services
# ============================================================================

# BFF (Backend For Frontend)
BFF_IMAGE=docker.io/library/nginx:alpine
BFF_TAG=latest

# Public Nginx (API Gateway)
PUBLIC_NGINX_IMAGE=docker.io/library/nginx:alpine
PUBLIC_NGINX_TAG=alpine

# ============================================================================
# Frontend
# ============================================================================

# Frontend Service
FRONTEND_IMAGE=docker.io/library/nginx:alpine
FRONTEND_TAG=alpine

# ============================================================================
# 拉取所有鏡像的指令
# ============================================================================
# podman pull ${API_USER_IMAGE}:${API_USER_TAG}
# podman pull ${API_ORDER_IMAGE}:${API_ORDER_TAG}
# podman pull ${API_PRODUCT_IMAGE}:${API_PRODUCT_TAG}
# podman pull ${BFF_IMAGE}:${BFF_TAG}
# podman pull ${PUBLIC_NGINX_IMAGE}:${PUBLIC_NGINX_TAG}
# podman pull ${FRONTEND_IMAGE}:${FRONTEND_TAG}

# ============================================================================
# 生產環境範例
# ============================================================================
# API_USER_IMAGE=registry.example.com/microservices/api-user
# API_USER_TAG=v1.2.3
# 
# API_ORDER_IMAGE=registry.example.com/microservices/api-order
# API_ORDER_TAG=v2.0.1
# 
# API_PRODUCT_IMAGE=registry.example.com/microservices/api-product
# API_PRODUCT_TAG=v1.5.0
# 
# BFF_IMAGE=registry.example.com/microservices/bff
# BFF_TAG=v3.1.0
# 
# PUBLIC_NGINX_IMAGE=docker.io/library/nginx
# PUBLIC_NGINX_TAG=1.25-alpine
# 
# FRONTEND_IMAGE=registry.example.com/microservices/frontend
# FRONTEND_TAG=v4.0.2

# ============================================================================
# 開發環境範例（本地建置）
# ============================================================================
# API_USER_IMAGE=localhost/api-user
# API_USER_TAG=dev
# 
# API_ORDER_IMAGE=localhost/api-order
# API_ORDER_TAG=dev
# 
# API_PRODUCT_IMAGE=localhost/api-product
# API_PRODUCT_TAG=dev
# 
# BFF_IMAGE=localhost/bff
# BFF_TAG=dev
# 
# FRONTEND_IMAGE=localhost/frontend
# FRONTEND_TAG=dev
