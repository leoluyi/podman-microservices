# ============================================================================
# 高可用配置 — 各服務副本數
# ============================================================================
# 管理腳本（start-all.sh, stop-all.sh, status.sh 等）會讀取此檔案
# 修改後重新執行 start-all.sh 即可生效
#
# 注意：調整副本數後，也需要同步更新
#   configs/ssl-proxy/conf.d/upstream.conf 中的 upstream backend 列表

API_USER_REPLICAS=2
API_ORDER_REPLICAS=2
API_PRODUCT_REPLICAS=2
BFF_REPLICAS=2
FRONTEND_REPLICAS=2
