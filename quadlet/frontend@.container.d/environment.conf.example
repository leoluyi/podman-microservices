# 環境變數覆蓋 - Frontend
# 使用方式：
#   開發環境 - ./scripts/setup.sh dev（自動啟用）
#   生產環境 - 手動複製此檔並填入正確值，部署至：
#              ~/.config/containers/systemd/frontend.container.d/environment.conf

[Container]
# Debug 端口（開發環境，只綁定 localhost）
PublishPort=127.0.0.1:8100:80
